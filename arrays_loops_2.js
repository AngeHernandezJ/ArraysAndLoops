console.log("Number divided into halves");
const NUMBER = 7; //Aquí se cambia al número deseado
console.log(NUMBER);
console.log([NUMBER/2,NUMBER/2]);

/*//Segunda opción
const N1 = 4; //Número a evaluar
let res = [];
function dividir (num) {
    const half = num/2;
    res.push(half);
    res.push(half);
    console.log(res);
}
dividir(N1)*/;